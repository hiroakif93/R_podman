{
  "name": "project",
  "build": {
    "dockerfile": "Containerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspaces/project",
  "remoteUser": "root",
  "postCreateCommand": "R -q -e \"if (!requireNamespace('renv', quietly = TRUE)) install.packages('renv', repos='https://cloud.r-project.org'); if (file.exists('renv.lock')) renv::restore(prompt = FALSE)\"",
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "quarto.quarto"
      ]
    }
  }
}